const MotionSectionXPlus = ({classNames, children}) => {
    return (
        <section
            className={classNames}
        >
            {children}
        </section>
    )
}

export default MotionSectionXPlus
