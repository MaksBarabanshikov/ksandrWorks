const MotionSectionX = ({classNames, children}) => {
    return (
            <section
                className={classNames}
            >
                {children}
            </section>
    )
}

export default MotionSectionX
